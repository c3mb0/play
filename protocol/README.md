# Port protocol (deferred)

No Erlang Port protocol is implemented at the Phase A/B checkpoint. The helper
will initially be a one-shot local mechanism; its stdout is raw PTY output.
Do not treat that CLI as the eventual versioned, framed session protocol.
